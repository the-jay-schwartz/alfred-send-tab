# Send Tab

Capture the current browser tab via the `st` keyword, then choose to open it in another browser or view its page source in an editor.

## Configuration

Change the keyword (default `st`) or your preferred editor (default Visual Studio Code) from the Workflow's Configuration.

## Notes

Viewing page source uses curl, so it won't carry your browser session — a page that requires login will show its login wall.
